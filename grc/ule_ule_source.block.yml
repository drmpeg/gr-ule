# auto-generated by grc.converter

id: ule_ule_source
label: IP over TS Packet Source
category: '[IP over TS (ULE)]'

parameters:
-   id: mac_address
    label: MAC Address
    dtype: string
-   id: filename
    label: DVB Config File
    dtype: string
-   id: frequency
    label: Receive Frequency
    dtype: string
-   id: call_sign
    label: Call Sign ID
    dtype: string
-   id: ping_reply
    label: Ping Reply
    dtype: enum
    options: [PING_REPLY_OFF, PING_REPLY_ON]
    option_labels: ['Off', 'On']
    option_attributes:
        val: [ule.PING_REPLY_OFF, ule.PING_REPLY_ON]
-   id: ipaddr_spoof
    label: UDP IP Address Spoofing
    dtype: enum
    options: [IPADDR_SPOOF_OFF, IPADDR_SPOOF_ON]
    option_labels: ['Off', 'On']
    option_attributes:
        hide_ipaddr: [all, '']
        val: [ule.IPADDR_SPOOF_OFF, ule.IPADDR_SPOOF_ON]
-   id: src_address
    label: Source IP Address
    dtype: string
    hide: ${ ipaddr_spoof.hide_ipaddr }
-   id: dst_address
    label: Destination IP Address
    dtype: string
    hide: ${ ipaddr_spoof.hide_ipaddr }

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import ule
    make: ule.ule_source(${mac_address}, ${filename}, ${frequency}, ${call_sign},
        ${ping_reply.val}, ${ipaddr_spoof.val}, ${src_address}, ${dst_address})

file_format: 1
